document.addEventListener('DOMContentLoaded', () => {
    const fileUploadButton = document.querySelector('.upload-btn');

    if (fileUploadButton) {
        fileUploadButton.addEventListener('click', () => {
            alert('Upload signature flow placeholder.');
        });
    }
});
